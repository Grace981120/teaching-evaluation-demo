# 课堂评价报告 · 交互 DEMO

参考 Figma「教评分析/提案」中的 `119:37904` 报告总览、`119:39025` 教学态度、`202:35964` 教学内容、`202:40665` 教学方法、`225:45354` 教学效果与 `225:44201` 教学素养画板，使用原生 HTML、CSS、JavaScript 实现。无需安装前端依赖。

## 启动

在此目录运行：

```sh
python3 server.py
```

打开 http://127.0.0.1:5178 。也可运行 `npm run dev`（需要已有 Node.js/npm 环境）。端口冲突时可用 `python3 server.py --port 5180` 指定其他端口。

## 公网部署与访问保护

公网地址：https://majestic-zuccutto-d8f0af.netlify.app 。Netlify 上通过 `netlify/edge-functions/access.js` 验证访问密码，报告页面及静态资源都受保护。登录会话有效期为 12 小时，使用签名的 HttpOnly、Secure Cookie；密码或签名密钥变更后旧会话失效。未设置服务端配置时拒绝访问。

服务端环境变量为 `DEMO_PASSWORD_SALT`、`DEMO_PASSWORD_HASH`（SHA-256 的 `salt:password` 十六进制摘要）和 `DEMO_SESSION_SECRET`（随机 32 字节以上密钥）。不要把密码、会话密钥或环境文件提交到仓库。

部署使用 `netlify deploy --prod`，配置文件会运行 `python3 scripts/build-site.py` 并打包访问保护函数。不要再仅通过 Drop 上传静态目录，否则不会包含访问保护。`dist/` 只包含对外发布的页面与资源，不包含源配置及辅助脚本。验证密码保护逻辑：`node scripts/test-access.mjs`。本机 `server.py` 保持用于开发预览，不启用线上密码验证。

## 已实现

- 课程总评、得分说明、五维雷达图，图例支持切换。
- 五维诊断卡片与指标摘要弹窗，可跳转关联证据。
- 四组证据切换、课堂行为图表、时间戳证据展开，支持方向键切换。
- 改进建议加入计划、完成标记、移除计划。
- 跟进记录编辑；计划和记录存储于当前浏览器 localStorage。
- JSON 报告下载和打印 / 保存 PDF 的浏览器入口。
- 桌面、平板、手机适配，原生对话框与键盘焦点支持。
- 教学态度独立页面：基础情况、肢体语言、教学姿态、巡堂轨迹、课堂管理。
- 基础指标记录弹窗、肢体语言时间拖动及悬停、姿态片段查看、巡堂区域选择、管理事件查看及图例筛选。
- 总览诊断地图继续打开弹窗；教学态度弹窗使用与详情页共享的数据，可跳转到对应模块。
- URL `#attitude` 与各模块锚点支持刷新、直接打开和浏览器前进后退。
- 教学内容独立页面：课程目标、课程重难点、学科特点、理论联系实际；目标与重难点节点支持查看单项或全部详情，案例时间点可查看课堂记录及关联知识。
- 总览教学内容诊断弹窗与详情页共享数据，支持跳转四个模块。`#content`、`#content-goals`、`#content-challenges`、`#content-features`、`#content-practice` 支持直接打开、刷新和前进后退；JSON 下载包含教学内容数据。

## 范围与数据

目前已实现报告总览及教学态度、教学内容、教学方法、教学素养、教学效果五个维度页面；课程实录提供三路课堂画面、量表评价与示例语音转写。所有分数、图表、时间戳和观察结论均为示例数据；没有连接课堂视频或后端服务。疑似代课等异常线索仅用于演示，不构成事实认定。

教学内容按新画板展示人工智能课程示例。分数统一为总览已有的 98.2；课程目标数量按图中实际节点统一为 3。保留原稿的 6 个重难点节点（含重复描述）以及各卡片重复的 00:01 案例；顶部摘要改为与教学内容相关的文案。

教学态度采用 130 分钟有效分析时长，区别于课表的 185 分钟。站立含走动，累计 121分07秒；走动 40分13秒；就坐 8分53秒。环图按互斥的原地站立、走动、就坐展示约 62%、31%、7%，避免设计稿占位比例重复计数。

保留设计稿的整体分数 92.1 和五维卡片分数；原稿雷达图使用的占位分数与卡片不同，DEMO 中已统一。综合权重按教学态度 25%、教学内容 20%、教学方法 20%、教学素养 15%、教学效果 20% 计算，结果为 92.11，显示为 92.1。

## 文件

- `index.html`：总览页面与弹窗容器。
- `styles.css`：视觉样式、响应式布局及打印样式。
- `app.js`：示例数据、图表及交互逻辑。
- `attitude-data.js`：教学态度页面及总览弹窗共享的示例数据。
- `attitude.js` / `attitude.css`：教学态度模块、图表、交互及响应式样式。
- `content-data.js`：教学内容详情、总览弹窗和 JSON 下载共享的示例数据。
- `content.js` / `content.css`：教学内容关系图、案例卡片、弹窗及响应式样式。
- `method-data.js`：教学方法页面、总览诊断弹窗和 JSON 下载共享的数据。
- `method.js` / `method.css`：教学方法六模块、分类切换、图表详情及响应式样式。
- `effect-data.js`：教学效果页面、总览诊断弹窗和 JSON 下载共享的数据。
- `effect.js` / `effect.css`：教学效果六模块、指标详情、曲线采样及响应式样式。
- `effect-chart-assets.js`：Figma 原始曲线的组合结果，可运行 `python3 scripts/build-effect-traces.py` 重新生成。
- `server.py`：仅绑定本机回环地址的静态文件服务器。

后续页面可复用现有维度数据、卡片、弹窗和色彩变量继续扩展。

## Figma 参数还原（2026-09-08）

### 当前文字优化版

中文现已统一使用系统苹方（PingFang SC），覆盖标题、正文、图表标签及弹窗；评分数字保留 CCtype。未安装苹方的设备使用系统中文字体回退。

按后续要求，在原稿还原基础上通过 `typography.css` 重新设计文字层级。页面标题 26px、模块标题 22px、卡片标题 16–18px、正文 14px / 24px、辅助信息 12–13px。标题使用深蓝灰 `#18263C`，正文使用 `#536176`，辅助文字使用 `#6D7A8C`，提示使用 `#8490A0`。评分保持独立数字字体；巡堂百分比统一为 CCtype 28px。标签文字加深以改善辨识度。手机端单独调整字号，已检查两页无横向溢出，诊断弹窗可正常打开。

以下表格保留原始 Figma 参数作为来源记录；当前文字样式以最后加载的 `typography.css` 为准。

本次通过 Figma MCP 的 `get_design_context` 读取两个画板的图层数据与截图，将准确参数集中在 `figma-tokens.css` 中；该文件在基础布局样式之后加载。

| 项目 | 原稿参数 |
| --- | --- |
| 正文 / 标题 | 阿里巴巴普惠体 2.0，Regular / SemiBold / Bold |
| 模块标题 | 24px / 36px，Bold |
| 正文 / 辅助正文 | 14px / 22px；AI 小结 14px / 24px |
| 总览评分 / 维度评分 | CCtype Medium，72px / 40px |
| 态度评分 / 基础计数 | CCtype Medium，28px |
| 主文字 / 次文字 / 辅助文字 | #1C1D1E / #55585C / #7F848A |
| 图表蓝 / 绿 / 玫红 / 橙 | #1D70F2 / #00CC7E / #E545D2 / #FF7626 |
| 外卡片 / 内卡片 / 标签圆角 | 16px / 8px / 4px |
| 桌面内容宽度 / 模块间距 | 1440px / 32px |
| Logo / AI 图标 / 得分说明图标 | 40×40px / 16×16.018px / 16×16px |

`assets/figma/manifest.json` 记录导出资源来源。Logo、得分说明图标、AI 图标、肢体语言曲线与巡堂轨迹均使用原稿资源。图表筛选和诊断弹窗保留；雷达图和姿态图继续使用上述一致的数据口径。肢体语言悬停从实际曲线取样，原稿未绘制的末段提示“暂无曲线数据”。手机端曲线可横向滚动查看。

字体来自本机已安装字体，已复制到 `assets/fonts` 供此本地 DEMO 使用。巡堂数字的原稿使用 DIN Medium，本机仅有 DIN Regular，因此该小范围暂使用同家族 Regular；其余主标题、正文和评分字体已匹配。新增交互入口与数据说明保留，所以部分卡片高度会随内容增长。

已在 1920px 桌面和 390px 手机宽度检查；验证了诊断弹窗及模块跳转、巡堂区域切换、课堂管理筛选与肢体语言滑动，页面无水平溢出，浏览器未记录 JavaScript 错误。

## 教学内容补充（2026-09-09）

通过 Figma MCP 读取 `202:35964`，沿用苹方、现有文字层级和配色；外卡片 16px、内部节点 8px、时间标签 4px 圆角。AI 图标、时间图标和关系图连接线直接使用 Figma 导出的 SVG，来源记录于 `assets/figma/content-manifest.json`。

已检查 1920px 桌面和 390px 手机布局，无横向溢出或失效图片；验证了目标与重难点详情、键盘打开节点、案例关联知识、总览诊断弹窗及模块跳转、锚点刷新、浏览器前进后退。教学态度路由正常，原有环形图和时序图的 4px 圆角保留。

## 教学方法补充（2026-09-09）

参考 Figma `202:40665`，实现教学行为、语言表达、教学提问、课堂板书、教学互动、引导课后学习六个模块。沿用苹方、26px 页面标题、22px 模块标题与现有颜色变量；外卡片 16px、内卡片 8px、环形图和时序色块 4px 圆角。环图复用教学态度的圆角扇形函数，原有参数和效果保持一致。

`#method` 及 `#method-behavior`、`#method-language`、`#method-questions`、`#method-board`、`#method-interaction`、`#method-homework` 支持直接打开和刷新。总览教学方法卡片继续先打开诊断弹窗，指标摘要与详情页使用同一份数据。JSON 导出增加 `method` 数据。

交互包括：教师/学生行为筛选、时序片段详情、语言指标详情、布鲁姆/4MAT 分类切换、提问案例、环图占比详情、S-T 与 Rt-Ch 说明，以及课后任务的已识别/未识别详情。分类支持方向键切换，图表与普通按钮支持键盘操作；手机端时序图保留可横向滚动的时间轴。

数据处理说明：

- 教学方法评分沿用总览 82.1，顶部摘要改为教学方法分析；行为小结与原稿环图统一为教师 88%、学生 12%。
- 语言表达第四张重复标为“高频词”的卡片改为“语气词”；词项、问题、4MAT 分布与课后任务详情为演示补充。
- 时序图保留原稿的分段位置与 150 分钟时间轴，区间由图中位置近似换算，累计时长按实际绘制区间计算。板书按原稿实际绘制的 6 段统一为 6 段、21分36秒，取代原稿与图不一致的“7次、8分钟”说明。
- 原稿行为占比、不同模块时序及 Rt-Ch 点位为独立示例，未接入同一份课堂分析数据；相关弹窗已说明，避免将多个重叠行为时长直接相加作为行为占比。

原始 SVG 来源保存在 `assets/figma/method-manifest.json`。AI、切换、说明、状态及箭头图标使用 Figma 资源；S-T 轨迹与 Rt-Ch 分区保留原始 SVG 图形，坐标轴文字使用苹方。已核对桌面 1920px 与手机 390px 的图标尺寸、图表圆角、资源加载及横向溢出，并验证分类切换、筛选、详情与总览跳转。

## 教学效果补充（2026-09-09）

参考 Figma `225:45354`，实现学习参与度、学情监测、学习金字塔、学生异常行为、学生动作、学生表情六个模块。沿用苹方及既有文字层级、颜色变量和卡片圆角；表情环图与学习行为时序色块为 4px 圆角。仪表图直接使用原稿带圆角的 SVG；较小的迟到/早退弧段保留原始端点几何。

`#effect` 与 `#effect-participation`、`#effect-monitoring`、`#effect-pyramid`、`#effect-anomalies`、`#effect-actions`、`#effect-expressions` 支持直接访问、刷新和总览弹窗跳转。JSON 下载增加 `effect` 数据。支持仪表指标、行为柱、时序片段、表情占比详情，以及四组曲线的悬停/时间滑块查看；实时动作与表情图例支持筛选，至少保留一条曲线。

AI 图标、仪表弧线、曲线的原始资源来源记录于 `assets/figma/effect-manifest.json`。曲线使用原始 SVG 路径和设计稿的重复/镜像组合；悬停取值由绘制路径采样，使用屏幕矩阵将嵌套 SVG 转回主图坐标。斜纹区间和无曲线路径的时刻提示“暂无分析数据”。桌面曲线跟随容器宽度，手机端使用可横向滚动的画布。

数据处理说明：

- 教学效果评分与总览统一为 93.8，替换画板复用的 92.1 占位分数；六段重复小结替换为对应模块的分析文案。
- 保留设计稿的 90% 整体参与度、实到 27 人、五项学情指标以及动作/异常/表情统计。它们是独立演示数据；总览课程仍是 19 人，曲线的 0–60% 坐标也不用于推算 90%/82% 汇总值，相关详情已注明。
- 学习行为百分比表示行为分布，不代表知识留存率；原稿整数合计 99%，分组保留约 67%/33%。
- 学习行为时序的时间按原稿色块位置近似换算，时长按各行实际区间求和，避免原稿“150分30秒”超过 150 分钟时间轴；分布占比和时序为独立图表示例。
- 表情标签作为可见表现的示例分类，不据此推断个人心理状态或学习水平。

已检查 1920px 桌面和 390px 手机布局，无页面横向溢出和失效图片；核对仪表资产尺寸、图表圆角，并验证曲线取值（含无数据区间）、图例筛选、零记录弹窗、指标详情、学习片段与总览模块跳转。

## 教学素养补充（2026-09-09）

参考 Figma `225:44201`，实现教学素养摘要与审辩思维、沟通能力、合作能力、课程思政四张分析卡片。复用教学内容的双列卡片、时间标签、课堂证据弹窗和响应式布局，沿用苹方、现有文字层级与 16px / 8px / 4px 圆角规范。手机端四张卡片依次纵向排列。

`literacy-data.js` 统一提供详情页、总览诊断弹窗和 JSON 导出数据；`literacy.js` / `literacy.css` 实现页面与交互。`#literacy`、`#literacy-critical`、`#literacy-communication`、`#literacy-collaboration`、`#literacy-values` 支持直接访问。总览诊断地图保留先打开弹窗的交互，可跳转对应卡片；八条时间点记录均可查看课堂记录与对应分析建议。

AI 图标与时间图标直接使用原稿 SVG，来源与渲染尺寸保存在 `assets/figma/literacy-manifest.json`。评分沿用总览 95.1，等级统一为优秀；替换原稿复用的教学态度摘要、四段小结占位文字以及与记录不符的“共7个知识要点”描述。保留原稿四张卡片重复的两条 00:01 记录，建议文案明确为教学建议，不将医疗案例直接认定为学生已经具备审辩、沟通或合作能力的证据。未接入课堂视频。

已检查 1920px 桌面与 390px 手机布局，无页面横向溢出或失效图片；核对 16×16.018px AI 图标、14×14px 时间图标与卡片圆角，验证八条证据的 Enter 打开 / Escape 关闭、手机弹窗、总览模块跳转与深层链接定位。

## 图表交互统一（2026-09-09）

移除肢体语言、学习参与度、抬头率、实时动作和实时表情五张折线图底部的滑块及常驻读数，统一使用 `chart-interactions.js` 的图内悬停提示。提示从原始曲线路径取样，展示时间与对应数据；多曲线只显示已开启的系列，无数据区间仍明确提示。移出图表、滚动页面或按 Escape 隐藏提示及参考线，手机可轻点查看，键盘可使用方向键、Home 和 End 切换时间。

`chart-interactions.css` 统一底部时间轴和柱状图名称为 12px 苹方。肢体语言时间轴改用 HTML 标签，避免 SVG 缩放造成字号变化。各时序色块沿用姿态图的纵向 1.5 倍放大、提亮和阴影；所有完整圆环的扇区在悬停时仅将外半径增大至 1.08 倍，内半径和内侧圆角保持不变，原有 4px 圆角和点击详情保留。键盘焦点使用相同动效，减少动态效果的系统偏好下关闭过渡动画。

已检查桌面与 390px 手机的轴标签字号、页面溢出、五张曲线数据及无数据提示、图例筛选，以及时序和圆环放大样式；浏览器未记录脚本错误。此节交互说明替代前文的底部时间滑块说明。

## 课程实录（2026-09-10）

参考 Figma `241:48807`，将原“课程详情”入口改为 `#recording` 独立页面。复用苹方字体、文字层级、16px 卡片与 4px 播放器圆角；三张课堂图片、控件图标和趋势曲线来自原稿，来源记录于 `assets/figma/recording-manifest.json`。保留原稿课程信息，出勤率按 50/80 修正为 62.5%；原稿占位标题、导语及重复题目替换为示例评价文案。

播放器以图片代替视频，支持模拟播放/暂停、倍速、进度定位、字幕、三路/教师画面切换、适应画面及全屏。语音转写为演示文本，支持搜索高亮、时间点定位、自动跟随与文本下载。四项量表各占 25 分，档位为 100/85/70/60/0；支持必填校验、实时计分、本地保存和刷新恢复，不向任何评价后台发送数据。邀请评价入口仅复制链接。

新增 `recording.js`、`recording-data.js`、`recording-assets.js` 和 `recording.css`。已验证宽屏与 390px 手机布局、图片加载、必填校验、88.8 分计分示例与刷新恢复、转写搜索和定位、播放与倍速、字幕、画面切换、侧栏折叠，以及原报告总览的诊断弹窗。

课程实录双栏高度随视口自适应，左右等高；播放器通过黑色背景填充剩余空间，图片保持比例。右侧内容内部滚动，底部保留 16px 圆角。底部曲线悬停显示对应时间的学生抬头率，并已移除页面上的演示提示、图片预览和本地保存提示文案。
