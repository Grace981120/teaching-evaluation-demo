import {bindChartHover} from './chart-interactions.js';
import {effectData as data,effectColors,effectTime,effectDuration,rowDuration} from './effect-data.js';
import {effectTraces} from './effect-chart-assets.js';
import {donutSegmentAttributes} from './attitude.js';
const asset=n=>`assets/figma/effect-${n}.svg`;
const note='<div class="dialog-note">DEMO · 本页为设计稿中的独立课堂示例，与总览课程人数不同。曲线及统计用于交互演示，尚未接入视频或统一分析数据。</div>';
function gauge(i){const g=data.gauges[i];return `<button class="effect-gauge" data-effect-gauge="${i}" aria-label="${g.name} ${g.value}%，查看详情"><span class="effect-gauge-plot"><img src="${asset('participation-imgEllipse1165')}" width="119.76" height="60" style="left:.12px;top:0" alt=""><img src="${asset(g.asset)}" width="${g.w}" height="${g.h}" style="left:${g.x}px;top:${g.y}px" alt=""><strong>${g.value}<small>%</small></strong><span>${g.name}</span></span></button>`;}
function axis(){return Array.from({length:11},(_,i)=>`<span>${effectTime(i*900)}</span>`).join('');}
function chart(key){const t=effectTraces[key],d=data.charts[key],h=t.height,W=t.width;return `<div class="effect-chart" data-effect-chart="${key}"><div class="effect-chart-scroll" tabindex="0" role="group" aria-label="${d.title}，窄屏可横向滚动"><div class="effect-chart-canvas" style="--plot-height:${h}px"><div class="effect-y-axis">${Array.from({length:d.max===60?4:5},(_,i)=>{const v=d.max-i*(d.max===60?20:10);return `<span>${v}${v||d.unit==='%'?d.unit:''}</span>`;}).join('')}</div><div class="effect-plot"><svg class="effect-trace" viewBox="0 0 ${W} ${h}" preserveAspectRatio="none" width="${W}" height="${h}" role="img" aria-label="${d.title}，Figma 示例曲线"><g stroke="#e9ecf0" stroke-dasharray="3 3">${Array.from({length:5},(_,i)=>`<path d="M0 ${i*h/4}H${W}"/>`).join('')}</g>${t.groups.map((s,i)=>`<g class="effect-series" data-series="${i}">${s}</g>`).join('')}<defs><pattern id="effect-gap-${key}" width="6" height="6" patternUnits="userSpaceOnUse" patternTransform="rotate(30)"><rect width="6" height="6" fill="white"/><path d="M0 0V6" stroke="#edf1f5"/></pattern></defs>${t.gaps.map(([a,b])=>`<rect x="${a*W}" y="0" width="${(b-a)*W}" height="${h}" fill="url(#effect-gap-${key})"/>`).join('')}<line class="effect-crosshair" x1="0" x2="0" y1="0" y2="${h}" stroke="#8490a0" stroke-dasharray="3 3" visibility="hidden"/></svg></div><div class="effect-time-axis">${axis()}</div></div></div>${d.labels.length>1?`<div class="method-legend effect-series-legend">${d.labels.map((l,i)=>`<button data-effect-series="${key}" data-index="${i}" aria-pressed="true"><i style="background:${effectColors[i]}"></i>${l}</button>`).join('')}</div>`:''}<div class="chart-hover-tooltip" role="status" hidden></div></div>`;}
function bars(key,unit='次'){const items=data[key],max=key==='learning'?45:key==='actions'?30:7;return `${key==='learning'?'<div class="effect-learning-groups"><span>被动学习（约 67%）</span><span>主动学习（约 33%）</span></div>':''}<div class="effect-bars effect-bars-${key}" style="--columns:${items.length}">${items.map((d,i)=>`<button data-effect-bar="${key}" data-index="${i}" aria-label="${d.name} ${d.value}${unit}，查看详情"><span>${d.value}${unit}</span><i style="height:${d.value/max*120}px;background:${effectColors[i]}"></i><span class="effect-bar-label">${d.name}</span></button>`).join('')}</div>`;}
function learningTimeline(){return `<div class="method-timeline-scroll" tabindex="0" role="group" aria-label="学习行为时序图，窄屏可横向滚动"><div class="method-timeline effect-learning-timeline">${data.learning.map((r,i)=>`<div class="method-timeline-row"><span class="method-row-name">${r.name}</span><div class="method-track"><span class="method-break" style="left:35%;width:14.5%" title="未分析片段"></span>${r.segments.map(([a,b],j)=>`<button style="left:${a/90}%;width:${(b-a)/90}%;--segment-color:${r.color}" data-effect-segment="${i}" data-index="${j}" aria-label="${r.name} ${effectTime(a)}至${effectTime(b)}，查看片段" title="${r.name} · ${effectTime(a)}–${effectTime(b)}"></button>`).join('')}</div><span class="method-row-duration">${effectDuration(rowDuration(r))}</span></div>`).join('')}<div class="method-axis">${axis()}</div></div></div>`;}
function expressionDonut(){let start=0;return `<div class="method-donut"><svg width="220" height="180" viewBox="0 0 220 180" role="group" aria-label="学生表情分布">${data.expressions.map((d,i)=>{const a=(start+d.value/2)/100*Math.PI*2-Math.PI/2,path=donutSegmentAttributes(start,d.value-.8,220,62,44);start+=d.value;return `<path ${path} fill="${effectColors[i]}" data-effect-expression="${i}" data-corner-radius="4" tabindex="0" role="button" aria-label="${d.name} ${d.value}%，查看详情"/><text x="${110+Math.cos(a)*85}" y="${84+Math.sin(a)*78}" text-anchor="middle" dominant-baseline="middle">${d.value}%</text>`;}).join('')}</svg></div><div class="method-legend">${data.expressions.map((d,i)=>`<button data-effect-expression="${i}" aria-label="${d.name} ${d.value}%，查看详情"><i style="background:${effectColors[i]}"></i>${d.name}</button>`).join('')}</div>`;}
export function initEffect({openDialog}){
const root=document.querySelector('#effect');
const bodies={participation:`<div class="effect-participation-layout">${gauge(0)}${chart('participation')}</div>`,monitoring:`<h3 class="effect-monitoring-title">学情监测 <span>实到：27人</span></h3><div class="effect-gauge-grid">${data.gauges.slice(1).map((g,i)=>gauge(i+1)).join('')}</div><h3 class="effect-subheading">抬头率</h3>${chart('monitoring')}`,pyramid:`<div class="effect-split effect-pyramid-layout"><div><h3>学习行为分布</h3>${bars('learning','%')}</div><div><h3>学习行为时序图</h3>${learningTimeline()}</div></div>`,anomalies:bars('anomalies'),actions:`<div class="effect-split"><div><h3>学生动作分布</h3>${bars('actions')}</div><div><h3>学生实时动作</h3>${chart('actions')}</div></div>`,expressions:`<div class="effect-split"><div><h3>表情分布</h3>${expressionDonut()}</div><div><h3>学生实时表情</h3>${chart('expressions')}</div></div>`};
root.innerHTML=`<section class="card attitude-summary"><div><h1>教学效果 <strong>${data.score}</strong><span class="tag">优秀</span></h1><p>${data.summary}</p></div><span class="attitude-summary-label">本课节分析</span></section>${data.sections.map(s=>`<section class="card section" id="effect-${s.id}" aria-labelledby="effect-${s.id}-title"><div class="section-heading"><h2 id="effect-${s.id}-title">${s.name}</h2></div><div class="attitude-insight"><img src="${asset('participation-img111111')}" width="16" height="16.018" alt=""><p>${s.summary}</p></div>${bodies[s.id]}</section>`).join('')}<footer><span><span class="status-dot"></span>DEMO · 设计稿示例 · 斜纹表示暂无分析数据</span><a class="text-button muted" href="#overview">返回报告总览 ↑</a></footer>`;
const show=(title,body)=>openDialog(title,body+note,'教学效果 / 分析详情');
root.addEventListener('click',event=>{
const b=event.target.closest('[data-effect-gauge],[data-effect-bar],[data-effect-segment],[data-effect-expression],[data-effect-series]');if(!b)return;const ds=b.dataset,index=Number(ds.index);
if(ds.effectGauge!==undefined){const g=data.gauges[Number(ds.effectGauge)];show(g.name,`<div class="basic-dialog-value"><strong>${g.value}<small>%</small></strong></div><p>${g.detail}</p>`);}
if(ds.effectBar){const d=data[ds.effectBar][index];show(d.name,`<div class="basic-dialog-value"><strong>${d.value}<small>${ds.effectBar==='learning'?'%':'次'}</small></strong></div><p>${d.detail|| (ds.effectBar==='learning'?`“${d.name}”是本课学习行为的一种。可结合时序图观察其出现的环节与持续时间。`:`本示例汇总记录“${d.name}”${d.value} 次。右侧实时曲线展示各时刻人数，为设计稿的独立示例，不与累计次数直接相加。`)}</p>${ds.effectBar==='learning'?'<div class="dialog-note">此处百分比表示学习行为分布，不代表知识留存率；原稿整数占比合计 99%，约数分组为 67% 与 33%。</div>':''}`);}
if(ds.effectSegment!==undefined){const r=data.learning[Number(ds.effectSegment)],[a,z]=r.segments[index];show(`${r.name} · 学习片段`, `<div class="content-evidence-time"><time>${effectTime(a)}–${effectTime(z)}</time><span>持续 ${effectDuration(z-a)}</span></div><p>此片段展示“${r.name}”学习活动。时间依据设计稿色块位置近似换算，具体课堂记录待接入。</p>`);}
if(ds.effectExpression!==undefined){const d=data.expressions[Number(ds.effectExpression)];show('学生表情 · '+d.name,`<div class="basic-dialog-value"><strong>${d.value}<small>%</small></strong></div><p>设计稿将此类可见表情标记为“${d.name}”。可结合课堂任务、学生表达与反馈进行观察，不据此认定个人心理状态或学习水平。</p>`);}
if(ds.effectSeries){const wrap=root.querySelector(`[data-effect-chart="${ds.effectSeries}"]`),selected=b.getAttribute('aria-pressed')!=='true';if(!selected&&wrap.querySelectorAll('[data-effect-series][aria-pressed="true"]').length===1){return;}b.setAttribute('aria-pressed',String(selected));wrap.querySelector(`[data-series="${index}"]`).toggleAttribute('hidden',!selected);wrap.querySelector('.chart-hover-tooltip').hidden=true;wrap.querySelector('.effect-crosshair').setAttribute('visibility','hidden');}
});
root.addEventListener('keydown',e=>{if(e.target.matches('path[data-effect-expression]')&&['Enter',' '].includes(e.key)){e.preventDefault();e.target.dispatchEvent(new MouseEvent('click',{bubbles:true}));}});
const caches=new Map();
function updateChart(wrap,minute){const key=wrap.dataset.effectChart,t=effectTraces[key],d=data.charts[key],svg=wrap.querySelector('.effect-trace'),x=minute/150*t.width;const unavailable=t.gaps.some(([a,b])=>minute/150>=a&&minute/150<=b);if(!caches.has(key)){const inverse=svg.getScreenCTM().inverse();caches.set(key,[...svg.querySelectorAll('.effect-series')].map(group=>{const points=[];group.querySelectorAll('path[stroke]').forEach(path=>{const matrix=inverse.multiply(path.getScreenCTM()),length=path.getTotalLength();for(let at=0;at<=length;at+=.75){const p=path.getPointAtLength(at),q=new DOMPoint(p.x,p.y).matrixTransform(matrix);points.push(q);}});return points;}));}
const labels=[];if(!unavailable)caches.get(key).forEach((points,i)=>{if(wrap.querySelector(`[data-series="${i}"]`).hasAttribute('hidden'))return;let nearest=null;for(const p of points)if(!nearest||Math.abs(p.x-x)<Math.abs(nearest.x-x))nearest=p;if(nearest&&Math.abs(nearest.x-x)<t.width*.004){const value=Math.max(0,Math.min(d.max,Math.round((1-nearest.y/t.height)*d.max)));labels.push(`${d.labels[i]}约 ${value}${d.unit}`);}});
const message=`${effectTime(minute*60)} · ${unavailable||!labels.length?'暂无分析数据':labels.join(' · ')}`;const line=wrap.querySelector('.effect-crosshair');line.setAttribute('x1',x);line.setAttribute('x2',x);line.setAttribute('visibility','visible');return message;}
root.querySelectorAll('.effect-chart').forEach(wrap=>bindChartHover({
  plot:wrap.querySelector('.effect-trace'),tooltip:wrap.querySelector('.chart-hover-tooltip'),max:150,
  inspect:minute=>updateChart(wrap,minute),clear:()=>wrap.querySelector('.effect-crosshair').setAttribute('visibility','hidden')
}));
}
