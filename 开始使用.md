# 教评报告 DEMO · 完整可编辑项目

本压缩包包含截至 2026-09-10 的全部页面源码、示例数据、图片、SVG、字体、资源来源记录、构建脚本和 Netlify 服务端访问保护代码。包含课程实录最新的自适应等高布局、右侧底部 16px 圆角和学生抬头率提示。

## 打开与修改

1. 解压整个压缩包，保留目录结构。
2. 在 VS Code、Cursor 或 Codex 中打开 `teaching-evaluation-demo` 文件夹。
3. 直接修改根目录的 HTML、CSS、JavaScript 文件。无需安装前端依赖。
4. 在项目目录终端运行 `python3 server.py`（需要 Python 3；Windows 可用 `py server.py`）。
5. 浏览器打开 http://127.0.0.1:5178/ ，修改并保存后刷新即可查看。

也可直接双击根目录 `index.html` 预览；推荐使用上述本地服务器以获得完整浏览器交互。端口被占用时运行 `python3 server.py --port 5180`。

## 常用修改位置

- `index.html`、`app.js`：导航、报告总览及公共交互。
- `attitude.*`、`content.*`、`method.*`、`effect.*`、`literacy.*`：五个教学评价维度页面。
- `recording.js`、`recording.css`：课程实录页面及样式。
- `*-data.js`：对应页面的文案和示例数据。
- `typography.css`、`figma-tokens.css`：字号、字体、颜色等规范。
- `chart-interactions.*`：统一图表交互。
- `assets/`：完整图片、图标、字体和 Figma 资源来源记录。
- `README.md`：详细实现说明和数据口径。

中文优先使用设备上的苹方字体；未安装苹方的设备会使用系统中文字体回退。播放器当前使用课堂图片，未包含真实视频。浏览器中已填写的评价、计划与记录存储在该浏览器中，不属于项目源文件，不会随压缩包迁移。

## 构建和部署

运行 `python3 scripts/build-site.py` 重新生成 `dist/`。包内附带当前版本的 `dist/` 供预览和部署参考；继续开发请修改根目录源码，再重新构建。

Netlify 配置在 `netlify.toml`，构建命令为 `python3 scripts/build-site.py`，发布目录为 `dist`。服务端密码保护由 `netlify/edge-functions/access.js` 实现。使用 Netlify Git 集成或支持边缘函数的 Netlify CLI 部署整个项目，才能一并启用密码保护；仅拖拽静态 `dist/` 不会安装边缘函数。

迁移至新的 Netlify 项目时，在其环境变量中设置 `DEMO_PASSWORD_SALT`、`DEMO_PASSWORD_HASH`、`DEMO_SESSION_SECRET` 并使其可用于 Functions/Edge Functions。SALT 和 SESSION_SECRET 应为各自独立的随机字符串；HASH 为 UTF-8 编码的 `SALT + ':' + 访问密码` 的 SHA-256 十六进制字符串。原站的环境变量保留在 Netlify 中，压缩包不包含账号登录状态或生产密钥；本地预览无需这些配置。

可运行 `node scripts/test-access.mjs` 验证访问保护逻辑（需要支持 Web Crypto 的现代 Node.js，如 Node.js 22）。

## 包含内容与校验

保留完整开发源文件和资源，并附有可重新生成的构建产物。未打包 `.git` 本地版本历史、`.netlify` 登录/部署缓存、环境密钥和系统临时文件；这些不影响解压后修改源码。现有 GitHub 仓库保留在线版本历史：
https://github.com/liutianyo-hue/teaching-evaluation-demo

`文件校验清单.json` 记录包内其他文件的 SHA-256，可用于核对传输完整性。
